services:
  - type: web
    name: mplads-satark
    runtime: node
    buildCommand: ""
    startCommand: node backend/server.js
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
